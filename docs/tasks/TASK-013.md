# TASK-013 本地模型接入

以玩家本机UE→llama.cpp→GGUF→Qwen3.5-4B取代两次串行大模型调用。前置规则/轻量分类保留unknown，检索可知知识并过滤游戏状态，再进行一次4B推理，返回结构化候选及台词。世界执行器拥有最终权限。

本单接通本地运行库、模型部署、UE异步接口、开发输入入口和真实模型测试；正式对话界面仍由015推进。CPU为兼容运行方式，GPU卸载按显存预算配置。运行库和权重置于项目Runtime/LocalAI，构建时作为NonUFS资源随包，不上传玩家对话，不运行时联网下载。

原013存档工作转存016，历史设计归档不改。用户已授权当前设计口径更新，当前DOCX同位修订并记录DSGN-001。
