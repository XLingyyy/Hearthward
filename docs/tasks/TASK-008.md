# TASK-008 个人背包基础数量、负重与行走减速

Owner：XLingyyy；当前会话单写。实现及本地验证完成；流程Blocked（Issue、独立评审未落实）。
分支 `codex/TASK-008-personal-inventory`，基于已推送TASK-007，堆叠开发。

## 设计依据与范围

GDD v0.3第7章，Q067—070、Q183—187、Q226—230：初始容量100抽象重量单位，禁止超载。
已接受初配：wood木材1、stone石材1、ore矿石2、meat肉0.5、arrow箭0.05。
使用稳定FName类型ID、正整数数量，内部以0.01重量单位的整数表示，避免浮点累加使2000支箭错误超重。
本单只实现角色本地普通物品容器的查询／整批增加／整批扣除；失败时不部分执行、不广播变化。
容量100；移速倍率1−0.1r，耐力消耗倍率1＋0.1r。实际接入已存在的常态行走350cm/s；尚无耐力系统，仅读取倍率，不虚构消耗。
HUD新增只读负重文本，既有动作HUD保留。开发命令增加／移除／查询用于验证，不形成正式拾取交互。

## 不包含

不执行004，不改资产、原设计、输入映射。空灰盒容器不确定故事开局物品。
不实现共享仓储、兄弟转交、装备计重、升级、关键任务物品、食物效果、丢弃生成、存档和请求去重。
CT-001仍为未批准草案；TASK-010保持原Backlog。本地同步调用每次代表一次新操作，不接收异步模型／网络重试。
后续领域消费者、保存／转移须先明确操作ID、epoch等契约，不将本单API冒充草案全部语义。

## 验收

- 构建通过，非零原生测试覆盖混合重量、精确满载、无副作用失败、扣除、极大数量和实例隔离。
- 两轮PIE验证实际组件、变化事件、容量拒绝、空／半／满载速度及卸载恢复；新会话无旧物资。
- HUD截图显示真实负重，动作计时及提示保持工作。
- 任务单、实现与证据完成后直接提交推送；不合并main。允许路径见 [机器任务单](TASK-008.json)。

工程参考：[Epic Lyra Inventory and Equipment](https://dev.epicgames.com/documentation/unreal-engine/lyra-inventory-and-equipment-in-unreal-engine)。
采用物品定义与容器数量分离的结构，不引入Lyra联机复制、装备能力或GAS依赖。
