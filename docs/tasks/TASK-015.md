# TASK-015｜背包和伙伴UI状态壳

基于GDD现行第11/14章、Q257—258及DSGN-001，复用009背包、012伙伴执行器和013本机模型，提供可操作的原生交流面板。临时T键在30米内打开，Enter发送、Esc关闭；输入独占焦点，关闭恢复移动/视角，背包与对话互斥。

显示实际负重、目标/三步、真实入库进度、思考、回复、受阻、取消及过期反馈。UI只查询公共接口和提交命令，不维护第二份库存，不生成伙伴/资源。伙伴运行来源标为开发夹具；文字回复来自真实模型。

全局建议尚无生成接口，本单显示明确不可用状态，不生成假建议、不自动刷新。关闭/取消沿用013开发测试行为，R20正式体验、R23正式键位保持OPEN；不新增长期记忆、全局建议生成或正式NPC。旧Tribe目录修正为现有Hearthward命名，仅追加原生UMG/Slate模块依赖。

验证T-025状态覆盖、输入焦点、边界输入、范围变化、UI互斥、真实模型发送与入库、跨PIE清理。交接记录实际结果及提交绑定，README替换旧UI缺项描述。用户授权实现、提交推送，不授权合并main；004不执行。
