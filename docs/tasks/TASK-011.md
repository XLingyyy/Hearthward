# TASK-011 距离交互、五秒读条和中断反馈

Owner：XLingyyy；用户授权执行本单及独立测试场景并提交推送。实现与本地验证完成；流程Blocked，尚无正式原型审批和独立评审。
承接已推送010，分支 `codex/TASK-011-distance-interaction`。

## 依据和实现

GDD v0.3 Q043/Q170：建造5实玩秒，移动/受伤中断，未完成不耗料。
复用006计时与007 HUD；新增交互发起组件和目标组件。目标显式配置距离，默认0不可交互，不猜正式范围。
开始、运行中和完成前检查目标有效性及距离。目标销毁、越界、移动和受伤均终止本次交互；暂停冻结计时。
计时完成且目标仍有效才发一次完成回调；回调仅表示交互条件满足，领域消费者仍须检查资源与实际结算。
回调前清理本次待执行目标，拒绝进行中重复启动，不让取消或旧目标产生迟到完成。
临时E键选择范围内最近的已配置目标，触发一次；无目标或失败显示反馈。Tab暂停行为沿用009。

## 隔离测试与未定点

正式配方、营地建造资格、完整选取/遮挡规则及键位表尚未确定。
生产目标默认距离0。开发命令显式创建PROTOTYPE_ONLY交互点，测试距离200cm；不改主地图，Shipping不注册命令。
PIE脚本在该点完成回调后尝试扣2木材，仅作为独立领域消费夹具；不生成建筑或定义最终配方。
未完成、重复请求、移动/伤害/越界/销毁取消不扣物；途中材料被用掉时完成回调也不假报建造成功。
本单用户执行授权支持本地夹具验证，prototype_approval保持空以保留正式Ready门槛，不冒填审批记录。
不执行004，不修改原设计、CT-001、资产、保存或正式营地交互规则。

## 验收

构建和既有动作原生测试通过；真实PIE验证距离边界、5秒、暂停冻结、移动与伤害中断、目标销毁/移远、重复启动、未完成不扣料、成功一次扣料与缺料拒绝。
截图核对实时读条、中断和计时完成反馈；重新PIE无旧目标/物资。
路径见[任务JSON](TASK-011.json)。

参考：[Epic Lyra Interaction](https://dev.epicgames.com/documentation/unreal-engine/lyra-sample-game-interaction-system-in-unreal-engine)。采用发起者与交互目标解耦；本单按距离选取，不引入Lyra GAS或将射线选取规则定为最终设计。
