# TASK-000｜采用工作流的检查单

本机准备更新见 [TASK-000 交接](../handoffs/TASK-000.md)。复合项只完成部分时保留未勾选。

- [x] 逐文件导入启动包，保留现有仓库规则，不覆盖他人工作。
- [ ] 确定真实角色、仓库URL、内部工程名与目录；填toolchain锁文件。
- [ ] 安装真实CODEOWNERS，并验证匹配与Owner批准要求。
- [ ] 配置main保护并用失败PR验证；记录可用功能与套餐限制。
- [ ] repo-policy跑通，再设为Required；试验脚本失败是否阻止合并。
- [ ] Git LFS安装、实际取回资产、双账号加锁/受阻/释放演练。
- [ ] 锁UE、编译器、SDK、插件版本；两机干净克隆编译、打开、打包、启动。
- [ ] 记录实际成功的构建和测试命令；测试数量为0不得当通过。
- [ ] 实际Issue建好并关联TASK ID，完成领取与交接演练。
- [ ] 新Agent会话读取正确任务、拒绝越写范围，并从交接继续。
- [ ] 数据、模型、素材与仓库可见性的分享边界获批准。
- [x] 更新PROJECT_STATE：只写上述真实结果，注明剩余NOT_RUN。

TASK-001的设计裁定可并行，但根规范／共享索引由集成人协调合并。
M0完成后执行 `python scripts/validate_repo.py --launch-ready` 检查字段是否齐全；
脚本不会替你登录GitHub验证权限，也不会用布尔字段证明游戏真的编译过。
