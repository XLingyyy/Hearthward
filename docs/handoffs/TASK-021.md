# TASK-021 伙伴导航交接

已完成分支实现与验证，尚未合并main，待用户验收021。分支codex/TASK-021-companion-navigation，堆叠于用户已验收020及验收记录34b51bce3f12a9d1a23a793ec874d33b5d7a0b5e；021范围提交38e613fe8ace26c576f2d4777a60e9d6fc436dae。用户明确授权自行推进、提交和推送；正式Issue与独立评审仍缺项，流程保持Blocked，不阻断授权实现。

伙伴从Actor改为Character，使用AIController、CharacterMovement和Recast导航。采集、返营、跟随、进攻共用NavigateTo；正常速度和接受距离沿用现有参数。可绕行继续原计划，不接受到不了目标的部分路径。近距离目标被墙遮挡时仍会绕行接近，视线检查继续阻止隔墙伤害。

取消、新命令、时间线失效及读档停止旧控制器路径和速度；路径不写入存档，恢复按保存的任务状态与当前导航重新求路。保存结构未变。无法前往资源点时返营等待；无法返营时保留实际携带物资，限制失败重试频率，通路恢复后继续入库。完成交付清除旧受阻原因。

现有开发场景缺少导航资产：创建伙伴时按已有碰撞网格范围创建临时NavMeshBoundsVolume，不保存地图；有原生导航边界的关卡直接复用。Config/DefaultEngine.ini启用Recast Dynamic（半径34cm、高180cm）。正式地图可使用关卡导航边界；未新增外部依赖或Content二进制资产。

验证：最终HearthwardEditor Development构建PASS；基础真实PIE20/20，动态受阻与进攻边界13/13；范围检查PASS。实际完成绕墙两趟8木材入库、库存守恒、取消停止、绕墙跟随、携物返营快照恢复及一次交付、封闭资源不可达、返程封堵携物保留与通路恢复、近距离隔墙不造成伤害、绕行后伤害结算、移动跟随目标重规划。结果及可复用脚本在docs/qa/evidence/TASK-021/，实现提交绑定见working-tree-binding.json。

首次运行的Python静态导航查询触发引擎CDO ensure，移除这项冗余查询后以真实轨迹验证绕行；最终两轮没有该ensure。开发导航边界Brush移动性警告已修正并重测。Zen缓存首次启动耗时约66秒后正常，未改全局缓存设置。截图为真实灰盒三维画面，未用UI插画代替场景。

验证使用独立UUID存档池和未保存的临时障碍，没有改动用户存档与地图资产。未重跑未修改的本地模型推理、完整玩法套件、Shipping或干净克隆；旧存档结构保持兼容但本单未另跑旧版本跨进程迁移测试。导航范围为当前开发场景，未制作全地图内容、攀爬/跳跃NavLink、群体避让与最终角色动画，TASK-004未执行。

操作仍是X跟随、Z等待、C协助进攻、T交流委托。独立地图测试命令见BUILD_AND_TEST的021节。后续可按设计推进营地建造等缺失玩法；不要把本任务的导航完工记成全游戏完成。
