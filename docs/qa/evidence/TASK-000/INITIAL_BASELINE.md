# 初始基线提交验证

日期：2026-09-17。范围：工作流启动包、GDD归档、TASK-003任务单；没有游戏实现。
本记录在首个提交前生成，tested_commit 为未提交工作区；首个包含本文件的提交标识本次快照。

- 已从用户原始 Word 读取正文、表格、18章及附录A—C；不使用 LibreOffice。
- `python -X utf8 scripts/validate_repo.py`：11个任务的结构、引用和归档检查完成，仅发现本记录尚未创建的链接错误；创建本文件后定向复核交接链接。
- 归档清单校验已通过；不重复单独计算或比较哈希。
- 用户Word与启动包归档字节不同；经OOXML正文／表格逐段比较，仅名称及第1.3节个人训练／作品集说明不同，玩法条款一致。保留两份原件，CURRENT明确用户文件为当前权威来源。
- 仓库辅助工具：沿用 LOCAL_SETUP 中的29项覆盖结果。上一阶段已修复两项Windows指针夹具并定向复测；本轮无工具代码变更，不重复运行。
- TASK-003：Backlog；T-026、UE编译、PIE、打包、两机复现、真实模型全部 NOT_RUN。
- main保护、CODEOWNERS真人分配、双账号LFS锁演练：NOT_RUN；不运行 --launch-ready 冒充完成M0。
- 首次提交前核验暂存范围、文本空白和LFS归档指针；不包含 .agent-local、凭据、原始ZIP重复包或生成缓存。
- 原工作流文件存在Markdown双空格换行，按有意排版保留；空白检查排除此类行尾空格后检查其他错误。

初始 main 创建及推送基于用户明确授权和 WORKFLOW 第20.1节；后续功能改动仍使用任务分支及PR。
