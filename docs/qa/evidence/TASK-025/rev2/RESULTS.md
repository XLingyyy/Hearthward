# TASK-025 增强版 v2 研发交付结果

按用户附件实现 A+B，分支 `codex/TASK-025-agent-rev2`。实现与研发验证完成；未合并main，用户复验与独立评审未完成，不标记用户验收通过。正式流程因Issue/Reviewer缺项保持Blocked。

## 交付内容

- 统一能力目录、严格目标Schema及真实token预算；自由输入先生成可修改的规范任务卡，一次确认后执行。补数量保留原条件，多目标和未解决限制阻止执行。
- 显式记忆、别名检索及来源引用；撤销回收活跃容量。长期硬规则需要确认，独立于Top-K；普通文字约定不冒充强制约束。
- 新采集、携带、交付分别计数；实际效果生成事件与持久回执。有限重试、受阻返营、返营失败保留物资，显式恢复。
- NPC实际到工作台，使用自有或明确授权的营地材料制作箭矢/绳索、维修自己持有的唯一装备；真实扣料、产出与耐久结算，玩家制作维修接口回归。
- schema2存档、真实旧025文件迁移、结算中保存保护、跨读档epoch隔离。旧模型回复、旧卡片与重复操作不重复写世界。
- 无模型时仍能手动建卡、查询库存、取消和重试；每轮最多一次生成，自动生成重试为0。

## 最终冻结评测

第三轮提示版本 `task025-agent-v2-6`。开发集与独立留出各60条，留出集首次运行后没有修改代码或提示；前两轮已暴露语料与失败轨迹保留在round1/和round2/，不重复充当独立评分。

| 指标 | 开发集 | 第三轮留出集 |
|---|---:|---:|
| 全场景结果 | 60/60 | 60/60 |
| 模型原始理解 | 53/60（88.33%） | 56/60（93.33%） |
| 校验后规范意图 | 60/60 | 60/60 |
| 完整支持目标实际达成 | 15/15 | 15/15 |
| 补充条件后实际达成 | 3/3 | 3/3 |
| 正常完整目标误拒绝 | 0/15 | 0/15 |
| 未确认副作用 | 0 | 0 |
| 错误可执行负例卡片 | 0 | 0 |
| 可追溯库存/直接回忆答复 | 9/9 | 9/9 |

留出集达到附件定义的95%完整目标、90%条件目标、≤5%误拒绝门槛。分母仅为本次冻结样本，不能外推任意自然语言可靠性。4条原始模型错误由校验层拒绝，仍计模型理解失败。已暴露失败定向回归7/7，单独报告，不加入留出分母。

同机、同权重、共同42条原有能力输入：原025实际结果26/42（61.90%），v2为42/42；原始理解34/42→37/42。原版维持其自动执行策略，新版按用户批准的一次确认策略执行。原版不可修改；运行中断的前29条和续跑13条均保留。B新增制作/维修在开发集另计9/9。

## 工程和实机验证

最新版源码重新运行并通过：Editor构建、29项项目原生测试、原025的15条真实模型回归（102检查）、35项多轮交互、开发60、已知失败7、新留出60、CPU实际执行3。

以下已在第二轮通过，第三轮未改对应领域/生命周期/UI实现，保留原版本证据：工作台129检查、故障101检查、生命周期37检查、真实120秒超时33检查、玩家制作58检查、维修59检查，以及物理键鼠采集任务卡和制作/维修/长期规则8项检查。关键故障重复三次。具体差异和复测范围见 [delta-validation.md](delta-validation.md)，不能将这些报告表述为第三轮整版本全量重测。

物理操作实际输入中文并点击发送、改数量、确认，验证取消、关闭旧卡和实际交付；见 [physical-ui.md](physical-ui.md)。离屏CaptureUI图仅作结构辅助。系统中文输入法候选窗组合输入未单独测试。UE启动既有Condition failed诊断原样保留；项目具名29项为通过。

## 本机性能

i7-13650HX、16GiB RAM、RTX4060 Laptop 8GiB，UE5.8.1灰盒PIE运行中，Qwen3.5-4B Q4_K_M、llama.cpp b10964，4096上下文/256输出。

| 路径 | 冷首条 | 暖机中位 | 暖机P95 | 整块GPU峰值 |
|---|---:|---:|---:|---:|
| Vulkan开发 | 12.18s | 3.15s | 5.11s | 5305MiB |
| Vulkan留出 | 9.87s | 3.18s | 4.90s | 5294MiB |
| CPU兼容（3条） | 77.18s | 70.48s | 73.24s | 2473MiB |

Vulkan暖机P95达到本机8秒目标，冷首条单列。CPU三次耗时77.18/73.24/67.72秒，均完成真实执行；仅2个暖样本，不作为稳定CPU分位估计。CPU版可执行程序和实际ngl=0由进程观察确认。

留出66次生成输入token中位2030、最大2081；输出中位51、最大188。CPU输入2029–2072、输出46–80。另有真实模板token超限拦截测试，未用字符数假冒token计数。

Vulkan留出 PIE帧时间P95（推理前/中/后）：17.47/18.00/17.46ms。

CPU PIE帧时间P95（推理前/中/后）：17.49/17.59/17.42ms。

显存是整块设备使用量，包含UE、模型及其他桌面负载，不是模型专属占用。PIE帧时间受编辑器和60fps上限影响，不等同Shipping性能。提交API最大31ms，确认/取消API低于此次计时分辨率；0不能解释为零耗时，也不等同屏幕像素更新延迟。三次实际HTTP静默超时均约120.016秒，无自动重放世界操作。

默认启动策略仍为CPU。本机体验复验使用 `-HearthwardAIBackend=vulkan -HearthwardAIGpuLayers=32`，启动说明见 [025交接](../../../../handoffs/TASK-025.md)。

## 交付边界与证据

当前为单伙伴、已知木材点、两种既有配方、唯一自有装备维修。未增加完整装备交换，新的游戏不会凭空发放弟弟装备；维修可用实际已有持有状态/存档，评测使用明确夹具。手动卡片没有通用可视化条件编辑器。旧档缺少历史物资来源，无法追溯重建已经丢失的归属。

Shipping、第二机器、独立人物自然度审查和用户复验未完成。词项检索和4B仍可能漏识别；样本通过不代表全表达保证。详见 [limitations.md](limitations.md)。

[summary.json](summary.json)由实际轨迹计算；各manifest保留真实运行时HEAD、源码差异、模型配置、脚本和样本绑定。最终提交关系见 [working-tree-binding.json](working-tree-binding.json)。历史失败见 [evaluation-failures.md](evaluation-failures.md)，复测命令及场景映射见 [README.md](README.md)。
