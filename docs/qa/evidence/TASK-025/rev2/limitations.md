# TASK-025 v2的实际边界

- 非Shipping灰盒原型。一个伙伴，一个已知安全木材点；两种既有制作配方；自有且恰好一件的ItemId装备维修。完整危险感知、开放世界导航、生活调度、逐件装备和生产经济不在本单。
- 新游戏不会凭空给弟弟发放装备。维修测试使用显式损坏装备夹具，未增加玩家向弟弟交换装备的完整玩法；游戏内可使用既有持有状态或恢复对应存档。制作可明确授权使用营地材料。
- 自由文本先形成任务卡，确认后才执行。少量词项护栏能拦截已识别歧义，无法证明所有自然语言条件都已理解。保留完整原话和未解决项；必要时结束澄清重述。普通文字约定不提供强制执行保证。
- 手动卡片可选已注册动作/物品并调整数量；来源和其他限制通过自由输入形成规范卡，当前没有通用可视化条件编辑器。
- 记忆以显式玩家记录、本人真实事件和营地观察组成；词项/别名检索，无Embedding、自动总结或反思。语义改写可能漏检。事件视图128条，当前命令操作账本独立保存。
- 旧档没有完整的物资取得来源账本。迁移保留旧进度并按旧携带信息恢复，无法追溯修正原版已经算错的交付归属。
- 原生测试启动阶段仍有原版本已存在的Condition failed诊断，完整原始输出保留；项目具名测试结果单独统计。
- GPU显存样本为整块设备使用量，包含UE、模型及其他桌面程序；不声称是模型专属占用。帧时间来自真实PIE Slate回调，受编辑器和帧率上限影响，不等同Shipping性能。
- CPU与Vulkan各用独立运行报告；Shipping打包、第二机器、独立人物自然度审查与用户复验未完成，不能用自动化通过替代。
- 任务分支允许提交推送，未授权合并。独立Reviewer和正式Issue仍缺项，正式状态Blocked。

- Python侧确认/取消计时在本次环境可出现0或约16毫秒的量化值；0表示低于计时分辨率，不表示零耗时。该项测API返回，不等同屏幕像素更新延迟。
- 物理UI测试使用Windows SendInput输入中文并实际点击；未独立覆盖系统中文输入法候选窗的组合输入。跨档旧面板、旧修订等由脚本PIE验证，不能算作这些流程的物理键鼠全覆盖。
- 第二轮留出heldout2-031回答了正确库存，但误进入澄清流程，历史报告保留FAIL。第三轮修正只读查询归类，并重新冻结独立留出集；不将已暴露的第二轮语料继续算作独立评分。任意表达仍可能漏识别，可用“结束本次澄清”清理草稿。
