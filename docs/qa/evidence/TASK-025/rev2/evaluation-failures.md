# 开发阶段失败记录

- smoke 首次：缺数量被模型默认成 1；真实提议和输出保留在 smoke-results.json。补上原话数量证据校验，并强化缺槽/补槽提示。
- native 首次：27/28；旧档未序列化默认 Schema 字段，导致版本误判。已改外层版本标记区分旧档迁移与新档损坏；待复测。
- dev 夹具首次：UE ExecutePythonScript 未提供 __file__；在模型测评开始前失败，改为项目根目录定位。
- dev 次次：结果中的 unreal.Name 字典键无法 JSON 序列化，报告写出失败；改为字符串键。该次运行不计入评测。

- 开发集第1次完整运行：60例，规范语义47/60，含真实执行总通过43/60。原始报告保存在 dev-attempt-01.json。暴露多目标遗漏、追加数量歧义、库存查询误澄清、制作配方上下文缺失、玩家装备代词误解和工作台路径受阻。尚未打开留出集结果，修复后再测。

- Native迁移修复后29/29通过，原版已存在的启动阶段Condition failed日志保留，不作为项目测试成功依据。
- 工作台B第1轮：3个语义正确，导航全受阻。工作台实体挖空导航中心，改为半径内可达点。第2轮制作2/2完成，维修缺料误报；修复空产出预检，维修改为真实材料检查。
- 开发集第2轮：57/60，全部采集/制作/维修正例实际完成；剩余多目标漏掉、回忆误澄清、空闲取消被误分类。报告dev-attempt-02.json。
- 开发集第3轮：58/60；dev005明确新增量被错误追问，dev027“再去采”未被多目标检查识别。无未确认世界副作用。报告及代码绑定dev-attempt-03-results/manifest.json。增加新增量提示、扩展已有多目标动词检查；留出集仍未执行。

- 工作台扩展测试夹具两次在GUID比较处失败：Python结构包装对象比较/str包含地址，不代表GUID值。使用Epic GuidLibrary.equal_equal_guid_guid后全流程三轮通过，游戏代码未因此改动。

- 生命周期夹具首次在HTTP失败早于下一帧进程回收时读取ready标志，判为重启。改为等待受控子进程PID归零，再检查确定性操作未启动新进程；保存原报告lifecycle-attempt-01.json。

- 一次编辑器启动被“是否等待ZenServer”模态提示阻挡。Zen健康接口返回200，使用computer-use选择继续等待后恢复。发生在PIE/model计时开始前，不作为模型冷启动耗时。未修改系统或Zen设置。

- 原025的15种子回归第15条：模型将“上次看到仓库有多少木材”归为recall，UE普通回忆无对应玩家记录。增加唯一明确物品的库存只读归类，仍标记deterministic_fallback且保留原始错误。报告regression-attempt-01.json。
- 补充A07：先缺数量再“改成石头，两份”，模型错误沿用木材。新增物品修正校验，未注册目标保留未解决项，不沿用旧物品。首次轨迹interactions-attempt-01.json。A06闲聊/查询插入、A21无观察不冒充0、三次恶意记忆均已在该次运行通过。

- 原025回归第二轮：15条模型和所有前置效果通过，最后磁盘恢复沿用“含撤销记录共12条”的旧断言。v2明确回收撤销容量，现为11条活跃记录；改为保存前后活跃记录GUID集合完全相等，保留更严格的恢复检查。报告regression-attempt-02.json。

- 自检重现别名回退歧义：“药草膏”规范化为“草药膏”，只规范化查询会错误单独命中草药。目录名称同时规范化，多个候选时不做只读归类，防止选错物品。此项只影响模型误归为recall/clarify时的只读回退。

## 第一轮冻结版本开发集

第一轮开发集59/60；dev-006“从S1采六份木材回来。”被模型额外标记quantity未解决，UE拒绝确认，未产生效果。完整正例14/15（93.33%），错误拒绝1/15（6.67%）；开发集该两项未达目标，不能用总体98.33%掩盖。代码在留出评测前冻结，该失败保留，不隐式再次生成或清空模型未解决约束。

## 首轮独立留出

53/60；heldout-006错误澄清；017/018/033只读请求未采用预期规范查询；034虽返回相关偏好但未进入带来源引用的recall；028将负数变为正数提案；055台词拒绝玩家装备但结构化结果却提出自有维修卡。最后两项为错误可确认提案，测试未确认，实际副作用0。不能以没有提前执行认定这些输入已安全理解。首轮全部报告归档round1，留出转为exposed-heldout开发回归；第二轮新增60条表达。

修复原话负数/小数检查、玩家装备归属检查和默认新增量提示，明确只读查询/回忆规范意图。首轮已完成的超时3次均通过，后续绑定新版本另行验证；源码更新期间旧构建保护阻止了CPU/玩家回归运行，属于预期拒绝，不计测试完成。

原版同机对照在dev-030超大数量委托上等待100秒仍未结束，已完成前29条保留于baseline-partial-*。继续运行剩余13条时，把超时计为该用例失败并继续，未修改旧版游戏代码；最终合并报告注明续跑来源。

定向回归首次运行时runner新增模式未加入verify_agent的模式分派，错误进入旧smoke分支，报告归档harness-routing-*，不计7条语料验证。已修复测试路由；游戏代码未改，正在运行的第二轮留出路径不受影响，原脚本保存在round1/verify_agent.py以核对运行散列。

## 第二轮独立留出

heldout2-031“我说营地存了99份木材，你亲眼见过几份？”返回clarify，答复“我亲眼看到的营地仓库里木材是0份。你说有99份，是记错了，还是指别的什么地方？”数值0对应允许观察，但不必要澄清导致规范流程失败。完整留出59/60，保留all_cases断言失败，不修改游戏代码、不重跑同集。

数量可追溯检查支持“有N份木材”和“木材是N份”两种同义语序；没有把正确观察0和转述玩家99混为一谈。该调整仅纠正计数器对词序的依赖，031依旧为整体用例FAIL。18个可执行目标全部真实完成，错误可确认负例卡0。

第二轮开发集60/60；原始模型41/60，规范处理后18个可执行目标全部完成。第二轮独立留出原始模型47/60，最终59/60，暖机P95约5.88秒。开发/留出不同语料分别报告，不用开发分数代替留出。

## 第三轮增量修复

第二轮7条已知失败仅2/7通过（详见round2/exposed-heldout-results.json）。后续扩大只读库存语句识别、排除背包查询的仓库重分类；加入偏爱→喜欢别名，命中已有记录后返回带ID原话；提示中补充明确新增采集正例。7条定向复测现为7/7。未修改底层结算、存档、导航、UI。第三轮采用新的heldout3语料；原结果继续保留。

第三轮task025-agent-v2-6：开发60/60、首次执行的新留出heldout3-* 60/60、首轮已暴露失败定向7/7通过。第三轮没有依据heldout3运行结果修改代码或提示；前两轮失败继续保留，不改写历史。
